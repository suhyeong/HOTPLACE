<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/maincentercolor</item>
        <item name="colorPrimaryDark">@color/maincentercolor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="WelcomeTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/maincentercolor</item>
        <item name="colorPrimaryDark">@color/maincentercolor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <!-- welcome page EditText style -->
    <style name="welcome_EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:colorControlNormal">@color/white</item>
        <item name="android:colorControlActivated">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">#a6ffffff</item>
        <item name="android:textColorHighlight">@color/colorPrimaryDark</item>
    </style>

    <!-- welcome page Checkbox style -->
    <style name="CheckBoxStyle" parent="Theme.AppCompat.Light">
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <!-- sign up page EditText style -->
    <style name="signup_EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:colorControlNormal">#999999</item>
        <item name="android:colorControlActivated">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textColorHint">#999999</item>
        <item name="android:textColorHighlight">@color/mainorange</item>
    </style>

    <!-- Base application activity animation left, right -->
    <style name="CustomActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/white</item>
    </style>

</resources>
